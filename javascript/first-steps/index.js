function multiply(x, y) {
    return x * y;
}

function yell(string_to_caps) {
    console.log(string_to_caps.toUpperCase())
}

function longerThan(first_array, second_array) {
    return first_array.length > second_array.length;
}

yell("hi sir, welcome!");

const divide = (x,y) => x / y;

const whisper = upper_string => {
    return upper_string.toLowerCase();
}

const shorter_than = (first_array, second_array) => {
    return first_array.length < second_array.length;
}